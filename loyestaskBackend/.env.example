# Variables de entorno para Railway

# Base de datos MongoDB (Railway MongoDB Plugin)
# Railway automáticamente proporciona esta variable cuando agregas el plugin de MongoDB
DATABASE_URL=${{MongoDB.DATABASE_URL}}

# JWT Secret (generar uno seguro de al menos 32 caracteres)
JWT_SECRET=tu_jwt_secret_muy_seguro_de_al_menos_32_caracteres

# Frontend URL (se actualizará con la URL de Railway del frontend)
FRONTEND_URL=${{Frontend.RAILWAY_PUBLIC_DOMAIN}}

# Configuración de Email para DESARROLLO (Mailtrap)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=tu-mailtrap-user
SMTP_PASS=tu-mailtrap-pass

# Configuración de Email para PRODUCCIÓN (Brevo)
BREVO_API_KEY=tu_clave_api_de_brevo_aqui
FROM_EMAIL=admin@loyestask.com

# Puerto (Railway lo asigna automáticamente)
PORT=${{RAILWAY_PUBLIC_PORT}}

# NODE_ENV para producción
NODE_ENV=production
